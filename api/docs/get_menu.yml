menu docs
---
tags:
 - Getting all items on menu
parameters:
  - in: body
    name: body
    required: true
    type: string
    description: This route is used to get all ideas on the menu. 
responses:
  200:
    description: Item successfully placed. JWT token generated.
    schema:
      id: user
      properties:
        item_name:
          type: string
  400:
    description: item exits already